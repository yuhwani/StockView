# 📈 StockView — 나만의 주식 투자 판단 도우미

한국 주식(KOSPI/KOSDAQ)과 미국 주식(NASDAQ/NYSE)을 머신러닝과 기술적 지표로 분석해,
**"지금 살까, 아니면 뺄까"** 를 판단하도록 돕는 개인용 웹 앱입니다.
종목을 검색하면 가격 차트, 다음 거래일 상승/하락 ML 예측, 그리고 여러 신호를 종합한
**매수 · 관망 · 매도 판단**과 그 근거를 보여줍니다.

> ⚠️ 주가는 100% 맞출 수 없습니다(특히 단기). 이 도구의 신호는 ML 예측과 기술적
> 지표(추세·RSI·모멘텀)를 종합한 **참고용 판단**이며 틀릴 수 있습니다. 최종 결정과
> 책임은 본인에게 있습니다. 그래서 과신을 막기 위해, 신호와 함께 **이 모델이 과거에
> 실제로 얼마나 맞췄는지(정확도·베이스라인 대비 우위)** 도 항상 같이 보여줍니다.

## 구조

```
StockView/
├── backend/          # Python + FastAPI
│   ├── data.py       # FinanceDataReader로 한국+미국 주식 데이터 수집
│   ├── features.py   # 기술적 지표 → ML 피처 (이동평균, RSI, 변동성 등)
│   ├── model.py      # RandomForest 학습 / 예측 / 시간순 백테스트
│   └── main.py       # API 엔드포인트
└── frontend/         # React (Vite) + Recharts
    └── src/
        ├── App.jsx
        └── components/  # SearchBar, PriceChart, PredictionPanel
```

## 실행 방법

### 가장 간단 — `make` (권장)

```bash
make install   # 최초 1회: 백엔드 venv + 프론트 node_modules 설치
make           # 백엔드(:8000) + 프론트(:5173) 동시 실행, Ctrl-C로 둘 다 종료
```

브라우저에서 **http://localhost:5173** 접속.

| 명령 | 설명 |
|------|------|
| `make` | 백엔드 + 프론트 **동시 실행** |
| `make backend`  | 백엔드만 (코드 변경 시 자동 리로드) |
| `make frontend` | 프론트만 |
| `make install`  | 의존성 설치 |
| `make clean`    | venv / node_modules 삭제 |

> Windows에 `make`가 없다면: `winget install ezwinports.make` 설치 후 **터미널을 새로 열면** `make`가 잡힙니다.
> Makefile은 셸을 Git Bash로 고정하므로 Git이 설치돼 있어야 합니다.

### 수동 실행 (make 없이, 터미널 2개)

```powershell
# 터미널 1 — 백엔드
cd backend
./venv/Scripts/python.exe -m uvicorn main:app --port 8000 --host 127.0.0.1

# 터미널 2 — 프론트엔드
cd frontend
npm run dev
```

(최초 설치: `cd backend && python -m venv venv && ./venv/Scripts/python.exe -m pip install -r requirements.txt`,
그리고 `cd frontend && npm install`)

## API

| 엔드포인트 | 설명 |
|-----------|------|
| `GET /api/search?q=삼성` | 종목명/코드 검색 (한국+미국, 예: `삼성`, `AAPL`) |
| `GET /api/stock/{code}`  | 일봉 OHLCV 시세 (차트용) |
| `GET /api/predict/{code}`| ML 예측 + 백테스트 평가 지표 |
| `GET /api/preview/{code}`| 목록용 빠른 예측 (방향+확률만) |
| `GET /api/news/{code}`   | 최근 뉴스 헤드라인 (한국=네이버, 미국=구글 뉴스) |
| `GET /api/lists`         | 추천 목록 카탈로그 |
| `GET /api/list/{id}`     | 목록별 종목 (예: `krx_cap100`, `us_cap100`, `krx_gainers`) |

## 투자 판단 기준 (매수 / 관망 / 매도)

여러 신호에 **점수를 매겨 합산**하고, 합계 점수로 행동을 정합니다. (모든 근거는 화면에 그대로 표시)

| # | 기준 | 점수 | 데이터 출처 |
|---|------|------|------------|
| 1 | **ML 예측** (다음날 상승확률) | ≥55% +2 / ≥52% +1 / ≤48% −1 / ≤45% −2 | RandomForest (아래 참고) |
| 2 | **추세** (20·60일 이동평균) | 둘 다 위 +1.5 / 둘 다 아래 −1.5 | 시세 |
| 3 | **RSI** | ≤30 과매도 +1 / ≥70 과매수 −1 | 시세 |
| 4 | **모멘텀** (최근 5일) | +3%↑ +0.5 / −3%↓ −0.5 | 시세 |
| 5 | **밸류에이션** | PER<10 +0.5 / PER>40 −0.5 / PBR<1 +0.5 / PBR>5 −0.3 | 네이버(KR)·stockanalysis(US) |
| 6 | **수급** (외국인·기관 순매수) | 동반 순매수 +1 / 동반 순매도 −1 | 네이버 (KR만) |
| 7 | **뉴스 감성** | 긍정 우세 +1 / 부정 우세 −1 (헤드라인 사전 분석) | 네이버·구글 뉴스 |
| 8 | **재료(이벤트)** | 자사주매입·증설·최초기술·대형수주 등 +0.5씩(최대 +1) / 악재 −1 | 뉴스 키워드 |
| 9 | **애널리스트 의견** | Buy +0.5 / Sell −0.5 | stockanalysis (US만) |

**합계 → 행동**: `≥3` 매수 우위 · `≥1` 약한 매수 · `−1~1` 관망 · `≤−1` 약한 매도 · `≤−3` 매도 우위

**신뢰도 보정**: 그 종목에서 ML 모델의 백테스트 `edge`(베이스라인 대비 우위)가 0 이하이면
신뢰도를 낮추고 경고를 표시 → 과신 방지.

**손절·목표가**: 변동성(ATR 14일) 기반. 손절 = 현재가 − 1.5×ATR, 목표 = 현재가 + 2×ATR (손익비 ≈ 1.3:1).

### ML 예측 모델

1. 과거 일봉에서 피처 생성 — 단기/중기 수익률, 이동평균 대비 위치, 변동성, 거래량 변화, RSI, 최근 가격대 내 위치.
2. "다음날 종가 > 오늘?"을 타깃으로 RandomForest 학습.
3. **시간순** 앞 80% 학습 / 뒤 20% 검증 (시계열이라 셔플 금지).
4. 검증 정확도를 베이스라인과 비교해 `edge` 계산 → edge>0 이어야 모델이 의미 있음.

### 한계와 다음 단계

- **재료 감지(8번)는 뉴스 제목 키워드 기반의 거친 추정**입니다. "자사주 매입·증설·최초 기술·좋은 공시"를
  정확히 잡으려면 **DART 전자공시 API**(무료, 키 발급 필요) 연동이 필요합니다.
- **미국 수급**(기관 순매수)은 무료 일별 데이터가 없어 미반영입니다.
- 단기 주가 예측은 본질적으로 한계가 있습니다. 이 신호는 **참고용**이며 최종 판단은 본인 몫입니다.

### ⚖️ 검증 결과 — "기술적 근거의 예측력은 ≈ 동전던지기" (2026-06 측정)

`factor_backtest.py`(근거별 IC) + `factor_learn.py`(로지스틱 다변량 학습)로 코스피·코스닥
대형주 50종목·약 9만 종목-일을 시간순 검증한 결론:

- **기술적·가격 근거만으로는 미래 방향을 사실상 못 맞춘다.** 로지스틱 회귀 검증 AUC가
  5일 0.504 / 20일 0.520 / 60일 0.443 — 어느 기간도 베이스라인(항상 다수)을 못 넘었다.
  (RandomForest/XGBoost 앙상블도 정확도 ≈ 0.50으로 동일.) 이는 코드 문제가 아니라
  **효율적 시장의 본질** — 단기 방향은 거의 무작위다.
- 따라서 **사람이 정한 점수 가중치를 '학습된 가중치'로 교체해도 의미 없다** (학습할 신호 자체가 없음).
  단변량 IC에서 "추세가 역효과(평균회귀)"처럼 보이는 것도, 모델이 무작위 수준이라 신뢰할 수 없다.
- **결론: 점수제는 '예측기'가 아니라 '투명한 현황 체크리스트'로 쓴다.** 실제 기대수익의 원천은
  ① **재료(공시·뉴스 catalyst)** — 백테스트는 불가하나 인과적으로 주가를 움직임,
  ② **밸류에이션·재무(장기)**, ③ **리스크 관리(손절·목표가·변동성)**, ④ **투명성(근거 공개 → 사용자 판단)**.
  → 단기 방향 예측에 과신하지 말 것. (재검증 도구: `python factor_backtest.py`, `python factor_learn.py`)

## 커밋 컨벤션

커밋 메시지는 [Conventional Commits](https://www.conventionalcommits.org/) 형식을 따릅니다.
자세한 규칙·예시는 [COMMIT_CONVENTION.md](COMMIT_CONVENTION.md) 참고.
클론 후 한 번 `git config commit.template .gitmessage` 를 실행하면 커밋 시 양식이 자동으로 뜹니다.

## 나중에 해야 할 일 (TODO / 로드맵)

> ✅ **이미 구현됨 (요약)** — 실시간 이벤트 알림(텔레그램·로그인 자동시작·3분 주기)과 즉시 매매 시그널,
> 오늘의 추천(+평일 18시 1등 매일 알림), AI 종합 분석 자동(무료 Gemini, 재무·공시·섹터 입력해 경쟁력까지),
> 판단 근거 대폭 확장(재무 ROE·부채·영업이익률·매출성장 / 거시 VIX·금리 / 공매도(US) / 시장대비 상대강도·섹터 /
> 기술적 120일선·MACD·지지저항 / DART 공시·언론빈도), 판단 근거 쉬운 말 표현,
> **ML 확률 보정**·**RF+XGBoost 앙상블**, 모델비교(RF/XGB/LSTM)·walk-forward 백테스트.

### 1. 알림 고도화
- [ ] **관심종목 외 종목도 알림** — watch.json(관심·보유) 외에 시장 전체/추천/급등락 스캔에서
      큰 이벤트(대형 호재 공시·거래량 폭증·신고가 돌파)를 발굴해 알림. (추천 1등 알림이 1차 형태)
- [ ] **알림 조건 설정 UI** — 종목별 급등락 %·키워드·목표가 트리거를 화면에서 직접 지정 (현재 코드 고정).
- [ ] **목표가·손절가 도달 알림** — 보유/관심 종목이 내가 정한 목표가·손절가에 닿으면 즉시 알림.
- [ ] **알림 대시보드** — 지난 알림 이력·적중 여부를 모아 보고, 어떤 트리거가 잘 맞았는지 확인.

### 2. 판단 근거 추가 (아직 안 한 것)
- [ ] **재무 추세** — ROE·영업이익률이 개선/악화 중인지(분기 흐름), 어닝 서프라이즈(컨센서스 대비).
- [ ] **수급 추세 정교화** — 외국인·기관 '연속 순매수 일수'·금액 추세 (현재는 5일 순매수 여부만).
- [ ] **실적 발표일(어닝) 임박 경고** — 발표 D-day 표시 + 발표 전후 변동성 주의.
- [ ] **배당 매력** — 배당수익률·연속 배당성장 (안정·인컴 관점).
- [ ] **거시 지표 확대** — (VIX·금리는 완료) 유가·달러인덱스(DXY)·업종지수 추가.
- [ ] **섹터·테마 분석** — 같은 테마(2차전지·AI 등) 묶어 강세 섹터·대장주 식별.
- [ ] **미국 수급** — 무료 일별 기관매매 데이터가 없어 **보류** (13F는 분기별).

### 3. 기능 추가
- [ ] **종목 비교 화면** — 2~3개 종목을 나란히 놓고 신호·밸류·차트 비교.
- [ ] **포트폴리오 고도화** — 환율 반영 합계(USD→KRW), 수수료·세금 반영 손익, 실적 발표일 알림,
      차트에 내 매수/매도 시점 마커, 종목별 비중·분산도.
- [ ] **데이터 영속화(DB) + 계정(로그인)** — 계정·거래·관심종목을 localStorage → 서버 DB로,
      기기 간 동기화·개인화. (지금은 브라우저에만 저장)
- [ ] **회사 소개 자동 생성** (프로필 API 또는 LLM).
- [ ] **관심종목 일괄 예측 / 미리보기 캐싱.**
- [ ] **모바일 최적화(PWA)** — 폰에서 앱처럼 설치/푸시 알림.

### 4. 운영
- [ ] **워커 헬스체크/로그** — 자동 워커 상태·마지막 점검시각 기록, 죽으면 재시작.
- [ ] **신호 적중 추적** — 예측 vs 실제를 기록해 어떤 근거가 잘 맞는지 가중치 학습.
- [ ] **AI 분석 결과 캐싱** — 같은 날 같은 종목은 재사용해 속도·호출량↓.
- [ ] `.gitattributes`로 줄바꿈(LF/CRLF) 경고 정리.

---

### 🔁 이미 했지만 더 개선/다시 구현할 것
- [ ] **다기간 예측(하루/주/월/장기) 정확도** — 지금은 RandomForest 회귀. 피처·모델 개선·튜닝으로.
- [ ] **앙상블에 LSTM 추가 또는 가중평균** — 현재 RF+XGBoost 단순평균. 성능 좋은 모델에 가중.
- [ ] **타깃 재정의** — '다음날 상승'(이진) → '+0.5% 이상 의미있는 상승'으로 노이즈↓.
- [ ] **하이퍼파라미터 튜닝 + 피처 중요도 점검** — 과적합 줄이고 효과 없는 피처 정리.
- [ ] **진짜 동종업계(peer) 상대강도** — 현재 시장지수 대비만 → 같은 업종 평균 대비로 정교화.
- [ ] **KR 공매도·섹터명** — 현재 US만. KRX 데이터로 한국 공매도 비중·업종 분류 보강.
- [ ] **뉴스 감성 LLM화** — 키워드 사전 → Gemini가 헤드라인 감성을 0~1로 채점(무료).

### ↩️ 되돌리기/재검토 후보 (효과 대비 비용)
- [ ] **RF+XGBoost 앙상블** — 검증상 RF 단독과 거의 무승부인데 신호당 모델 6개 학습으로 느려짐.
      효과 대비 비용 재검토 → 'RF 단독+보정'으로 롤백하거나, 결과 캐싱으로 비용 상쇄.
- [x] **점수 가중치 수동값 검증 완료** — `factor_backtest`/`factor_learn`로 검증한 결과,
      기술적 근거의 예측력이 ≈0.5(동전던지기)라 **학습 가중치로 교체해도 의미 없음**.
      가중치는 직관값 유지하되 **점수제를 '예측기'가 아닌 '투명 체크리스트'로 사용**. (위 '검증 결과' 참고)
- [ ] **quick_signal 매 호출 재학습** — 워커가 트리거마다 모델을 새로 학습. 일배치 캐싱·경량화로.

### ⚖️ 비교·검증해서 결정할 것
- [x] **근거 기여도 백테스트 + 로지스틱 학습** — `factor_backtest.py`(IC) + `factor_learn.py`(다변량 학습)로
      각 근거의 예측력 측정 완료. 결론: 단기 기술적 예측력 ≈0.5 → 점수제는 투명 요약 용도. (위 '검증 결과')
- [ ] **앙상블 vs RF 단독** — 여러 종목 대규모로 정확도(Brier) vs 속도 A/B 후 결정.
- [ ] **isotonic vs sigmoid 보정** — 데이터량별로 어느 보정이 나은지 비교.
- [ ] **모델 비교 패널에 '앙상블' 행 추가** — RF/XGB/LSTM 옆에 앙상블 정확도도 표시해 한눈에 비교.
- [ ] **수익률 백테스트에 거래비용·세금 반영** — 수수료·슬리피지·세금 빼고 '진짜 수익'으로 전략 비교.
- [ ] **edge 높은 종목만 강조** — 백테스트에서 베이스라인을 이긴 종목만 추천/신뢰 표시.
- [ ] **추가 정직한 검증 (선택)** — 소형주(비효율 시장) 예측력, 재료 발생 후 단기 반응, 장기 가치투자 백테스트.
